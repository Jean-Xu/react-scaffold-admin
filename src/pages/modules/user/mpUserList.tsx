import React, { useEffect } from 'react'
import classNames from 'classnames'
import PageWrap from '../../../components/PageWrap/pageWrap'

interface MpUserListProps {
}

const MpUserList: React.FC<MpUserListProps> = (props) => {
  const {  } = props

  return (
    <PageWrap>
      <h2>小程序列表</h2>
    </PageWrap>
  )
}

export default MpUserList