import React, {  } from 'react'
import PageWrap from '../../../components/PageWrap/pageWrap'
import classNames from 'classnames'

interface UserListProps {
}

const BgUserList: React.FC<UserListProps> = (props) => {
  const {  } = props

  return (
    <PageWrap>
      <h2>后台用户列表</h2>
    </PageWrap>
  )
}

export default BgUserList
