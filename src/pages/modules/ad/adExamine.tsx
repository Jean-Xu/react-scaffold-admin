import React, { useEffect } from 'react'
import classNames from 'classnames'
import PageWrap from '../../../components/PageWrap/pageWrap'

interface AdExamineProps {
}

const AdExamine: React.FC<AdExamineProps> = (props) => {
  const {  } = props

  return (
    <PageWrap>
      <h2>广告审核</h2>
    </PageWrap>
  )
}

export default AdExamine